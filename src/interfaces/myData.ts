export interface typeServices {
  urlImg: string;
  title: string;
  description: string;
}
export interface typeArticles {
  id: string;
  autor: string;
  urlImg: string;
  title: string;
  description: string;
}
export interface PropsIcon {
  className: string;
}
